import React from "react";
import ReactDOM from "react-dom";
import "./index.css";  // Make sure your global styles are in a proper folder
import App from "./App";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
